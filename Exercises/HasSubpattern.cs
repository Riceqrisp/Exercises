using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading.Tasks;

namespace Exercises
{
    // Under construction part
    class HasSubpattern
    {
        public static bool HasSubpatternIndicator(string str)
        {
            for (int i = 0; i < str.Length; i++)
            {
                var currentSubstring = str.Substring(0, i);
                //Regex.IsMatch(str, );
            }
            
            return false;
        }
    }
}
